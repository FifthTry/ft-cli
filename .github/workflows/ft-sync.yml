on:
  push:
    branches:
      - main
      - release-fixes
    paths:
      - "**.ftd"
jobs:
  everything:
    name: ft-sync
    runs-on: macos-10.15
    steps:
      - name: Running ft-sync status
#      - uses: actions/checkout@v1
        run: |
          chmod +x ./ft-sync
          ./ft-sync status